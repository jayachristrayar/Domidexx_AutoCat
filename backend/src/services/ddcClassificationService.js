import { buildRecommendation, classPathLabels } from './ddcCandidateService.js';
import { buildJustification, validateDecision } from './ddcJustificationService.js';
import { MAIN_CLASSES } from './ddcKnowledgeBase.js';
export function recommendDdc(metadata={}){ const {analysis,recommended,alternatives,confidence}=buildRecommendation(metadata); const mainNumber=recommended?.main_class||null; const justification=buildJustification({analysis,recommended,alternatives,metadata}); const decision={ primary_subject:analysis.primary_subject, disciplinary_domain:analysis.disciplinary_domain, secondary_subjects:analysis.secondary_subjects, related_topics:analysis.related_topics, tools_or_technologies:analysis.tools_or_technologies, application_areas:analysis.application_areas, main_class: mainNumber?{number:mainNumber,label:MAIN_CLASSES[mainNumber],confidence}:null, classification_path: recommended?classPathLabels(recommended.number).map(p=>p.number):[], recommended_ddc: recommended?{number:recommended.number,label:recommended.label,confidence}:null, evidence:[...analysis.evidence_summary, ...(recommended?[{type:'ddc_knowledge_base',source:recommended.source,value:`${recommended.number} ${recommended.label}`}]:[])], justification, alternatives, provenance:['RULE_DERIVED','AI_ASSISTED','SOURCE_DERIVED'], requires_cataloguer_review:true, approval_status:'PENDING' }; const valid=validateDecision(decision); if(!valid.valid) throw new Error(valid.error); return decision; }
